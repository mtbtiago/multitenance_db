<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Domain:</strong>
  <%= @user.domain %>
</p>

<p><%= link_to "Contacts in #{@user.domain}", contacts_url(subdomain: @user.domain) %></p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
